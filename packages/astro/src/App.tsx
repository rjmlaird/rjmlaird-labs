export default function App() {
  return (
    <div style={{ padding: "4rem 1.5rem", maxWidth: 640, margin: "0 auto" }}>
      <h1 style={{ fontSize: "1.6rem" }}>exp-example</h1>
      <p style={{ color: "#9AA5B1", lineHeight: 1.6 }}>
        Replace this component with your experiment. This package exists as the
        template that <code>pnpm new:experiment &lt;slug&gt;</code> copies from —
        keep it small and generic.
      </p>
    </div>
  );
}
