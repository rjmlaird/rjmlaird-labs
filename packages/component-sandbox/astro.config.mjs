import { defineConfig } from "astro/config";

// Deployed independently as component-sandbox.labs.rjmlaird.co.uk
export default defineConfig({
  site: "https://component-sandbox.labs.rjmlaird.co.uk",
  output: "static"
});
